rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // FOR NOW: Allow all reads and writes
    // This is development only - add proper rules later
    match /{document=**} {
      allow read, write: if true;
    }
    
  }
}
